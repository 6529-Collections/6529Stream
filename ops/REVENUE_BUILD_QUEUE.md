# Revenue, commerce and client build queue

Owner: revenue builder / commerce orchestrator. Updated 15 September 2026.
This is a task-owned implementation queue. The integrator owns the shared
feature register, final current-stack execution, source integration and release.

Current branch: `codex/revenue-economic-continuity`, following escrow source
`ca63bcd9`, Safe test-oracle repair `0eaf1240`, and the integrator's four explicit
escrow imports (`0d7c1b57`, locally `f46b2697`).
There is no deployment, broadcaster or second native campaign in this lane.

## Delivered source: common runtime lifecycle and incident escrow

The common governed factory/runtime registry, explicit once-only Factory/Escrow
opt-in, active-only new assignments and credits, retained deprecated flushes,
and incident-only successor-wallet recovery are implemented. Constructors,
original public entries, captured credit identities, total owed and recipient
withdrawal authority are preserved. Final operator activation requires the
registry; initial unbound compatibility does not establish full-v1 activation.

The original RSR recovery records, selectors, events, recovery ID and
EIP-712 consent domain stay exact. Canonical retained manifest documents prove
old/new entries and affected recipient aggregates onchain. The terminal route
separately records class-4 FUNDS_RECOVERY and class-2 TERMINAL_FREEZE actions.
Its notice evidence names affected recipients, contributing credit events and
collections, and bound Artist authorities. Delivery and completeness of the
historical Artist coverage remain explicit governance/operations evidence.

Source `ca63bcd9` passed a 355-source ABI/storage closure, preserving 322 original
ABI entries and 20 original recursive storage roots. All 13 selected production
products fit. The integrator reviewed the full recovery batch and integrated it
as `663aa5e6`. The first focused run passed 14 of 17 escrow cases; three Safe
retry helpers mishandled Foundry's caught-revert return. The exact one-file
oracle repair `0eaf1240` was integrated as `0be87be5`. The integrator's retained
`escrow-retry-native2-20260915` run then passed all 17 escrow tests, with all 15
selected production products fitting. The original failed run is retained.

Two actual foundation governance/Safe cases remain authored but unexecuted
here. A genuine paid-sale-to-incident-recovery integration also remains required.
Local source captures are `.tmp-escrow-recovery-abi14`,
`.tmp-escrow-recovery-size15` and `.tmp-escrow-recovery-close16` under
`D:/repos/6529Stream`; these are development evidence, not release acceptance.

## Active: frozen royalty economic continuity

The Royalty Resolver now enumerates original protected configurations,
snapshots and explicit mode elections; a pristine successor imports them through
an exact delayed governance manifest and bounded permissionless chunks. Hash
origins and all original stored economics remain unchanged through replacement.
See [the continuity guide](../docs/guides/royalty-economic-continuity.md).

The final source passes a 1,032-source ABI closure. All 70 original ABI entries
and five original recursive storage roots remain exact. One selected production
capture finds all six products deployable: Royalty Resolver runtime 24,131 bytes,
with 445 bytes of margin. Ten unit/Safe cases and two actual-current
Artist/Core/paid-sale/custody cases are authored, not executed. The integrator
owns the Core pointer guard and combined runtime validation. Source review of
the new continuity worker remains separate from the compiler checks.

Captures are `.tmp-royalty-continuity-abi10`,
`.tmp-royalty-continuity-size11` and `.tmp-royalty-continuity-compat12` under
`D:/repos/6529Stream`. Completion requires actual guarded pointer replacement,
unchanged snapshot and zero/default-source behavior, Safe rollback/retry and
same-NFT custody settlement. A typed boundary alone does not establish that join.

## Ordered next batches

1. Finish Royalty continuity integration with the Core guard, then preserve
   frozen primary economics across the actual Manager/suite replacement seam.
   This is distinct from following an Artist successor and does not implement
   the separately denied inherited/global freeze changes.
2. Complete remaining supported settlement/sale behavior from the shared
   feature rows, including explicit reconciliation of ERC20 sale-family
   obligations and remaining curated/content selection combinations. Do not
   manufacture variants or count intentional later profiles as genesis gaps.
3. Client-only token/PLATFORM rights, exact TEMPLATE CLEAR/freeze and continuity
   workflows are transferred to the integrator's dedicated developer-client
   task. This lane supplies exact contract ABIs and economic invariants.
4. Dedicated current airdrop/distribution implementation is transferred to the
   integrator's burn/distribution task; this lane retains revenue contracts.

Root owns shared mint policy/ledger, concrete gates, burn-to-mint/redeem,
Core/entropy/governance and shared operator closure. Coordinate needed shared
interface changes before editing; do not duplicate those implementations.
The earlier denied ERC20 reveal-fee and inherited/global-freeze artifacts remain
unapplied; no indirect application or alternate-checkout bypass is authorized.

## Handoff discipline

Send a coherent commit, concrete behavior, focused checks, exact source/runtime
limits and remaining acceptance. Review at meaningful source boundaries.
Do not run duplicate native compilers or broad artifact regeneration.
