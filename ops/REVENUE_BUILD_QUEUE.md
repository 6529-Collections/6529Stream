# Revenue, commerce and client build queue

Owner: revenue builder / commerce orchestrator. Updated 15 September 2026.
This is a task-owned implementation queue. The integrator owns the shared
feature register, final current-stack execution, source integration and release.

Current branch: `codex/escrow-incident-recovery`, based on the clean
`642d017d` client handoff (parent `7209675b`).
There is no deployment, broadcaster or second native campaign in this lane.

## Active: common runtime lifecycle and incident escrow

Implement one governed common factory/runtime registry, explicit once-only
Factory/Escrow opt-in, active-only new assignments and credits, retained
deprecated flushes, and incident-only successor-wallet recovery. Preserve
constructors, original public entries, captured credit identities, total owed
and recipient withdrawal authority. Final operator activation must require
the registry; initial unbound compatibility does not establish full-v1 activation.

The original RSR recovery records, selectors, events, recovery ID and
EIP-712 consent domain stay exact. Canonical retained manifest documents prove
old/new entries and affected recipient aggregates onchain. The terminal route
separately records class-4 FUNDS_RECOVERY and class-2 TERMINAL_FREEZE actions.
Its notice evidence names affected recipients, contributing credit events and
collections, and bound Artist authorities. Delivery and completeness of the
historical Artist coverage remain explicit governance/operations evidence.

The complete source batch has passed a 355-source ABI/storage closure. All 322
original ABI entries and 20 original recursive storage roots remain exact.
One selected code-generation pass finds all 13 selected production products
within limits: Escrow runtime 22,427 bytes, Primary Resolver 24,288, Factory
9,170 and common registry 6,612. It is not a deployment or behavioral result.

Seventeen focused unit/Safe cases and two actual foundation governance/Safe
cases are authored. They cover consent/transfer/host integration, strict operator
activation, malformed capability replies, constructor-fixed gas inventory and
URI-independent content publication with exact governed references. C's bounded
runtime registry/binding/initialization review is clear; the complete recovery
transfer/manifest/test review remains pending. Native execution and a genuine
paid-sale-to-incident-recovery integration remain required.

Local captures are `.tmp-escrow-recovery-abi14`,
`.tmp-escrow-recovery-size15` and `.tmp-escrow-recovery-close16` under
`D:/repos/6529Stream`; the source handoff does not promote these to release evidence.

Acceptance must demonstrate exact captured owed debit, original wallet funds
untouched, native and supported token delivery, both consent paths/revocation,
deadline/nonce/replay failures, truthful notice timing, refused unsafe successor,
callback drift rollback and byte-identical Safe retry. Authored or typed-boundary
tests alone do not establish completed end-to-end integration.

## Ordered next batches

1. Frozen economic Resolver continuity: preserve frozen assignments/snapshots
   through actual Resolver replacement; distinct from following an Artist successor.
2. Existing token/PLATFORM rights and exact TEMPLATE CLEAR/freeze clients:
   actual compiler-selected ABI, original domain/caller/value, live observation,
   saved Safe retry/resume and readable workflow examples.
3. Dedicated current airdrop/distribution consumer and operator workflow:
   actual batch recipient/accounting and authorized zero-price distribution.
4. Remaining supported settlement/sale behavior from the shared feature rows,
   including explicit reconciliation of ERC20 sale-family obligations and
   remaining curated/content selection combinations. Do not manufacture new
   variants or count intentional later profiles as genesis gaps.

Root owns shared mint policy/ledger, concrete gates, burn-to-mint/redeem,
Core/entropy/governance and shared operator closure. Coordinate needed shared
interface changes before editing; do not duplicate those implementations.
The earlier denied ERC20 reveal-fee and inherited/global-freeze artifacts remain
unapplied; no indirect application or alternate-checkout bypass is authorized.

## Handoff discipline

Send a coherent commit, concrete behavior, focused checks, exact source/runtime
limits and remaining acceptance. Review at meaningful source boundaries.
Do not run duplicate native compilers or broad artifact regeneration.
