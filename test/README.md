# Tests

Foundry tests belong in this directory.

The current Gate A smoke baseline intentionally has no meaningful tests yet.
The next test work should add characterization fixtures before changing P0
protocol behavior, as described in `ops/ROADMAP.md`.
