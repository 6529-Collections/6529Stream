# VIEW preservation finality input and review profile

This source batch supplies fixed kernels for the existing selected combined
provider. It adds no provider, Registry, authority, signing domain, or finality
head. The host must dispatch canonical VIEW scopes before its other policy
selector, pass its original constructor-owned `Native.Config`, and preserve the
original Registry boundary on both prepared operations.

## Host API

The fixed `StreamFinalityViewPreservationOperationsV1` APIs are
`manifest(Config,Scope) -> bytes`, `inputs(Config,Scope,hash) ->
(StreamFinalityScopeInputs,bytes32,bytes32)`, `review(Config,Scope,hash) ->
ReviewFacts`, and `prepared(Config,Scope,hash,validatedComponents,bool) ->
(StreamFinalityScopeInputs,bytes32,bytes32,ReviewFacts)`. The last entry first
checks actual caller and runtime against the original Registry. Its `bool`
selects whether the same checked statement also supplies review facts.

`StreamFinalityViewPreservationComponentsV1.facts(Config,Scope,family)` supplies
the seven original metadata/render families. It binds each distinct family,
the complete current snapshot/root and source commitments, with actual
snapshot class2 lock; metadata also joins original WORK/RIGHTS seals, complete
Artist association and intent lock. The original reference and entropy
components remain independent actual scoped components.

`StreamFinalityViewPreservationMetadataV1.root`, `.snapshot`, and `.manifest`
provide the existing scoped root/snapshot/membership projections.
`StreamFinalityViewPreservationConfigurationV1.catalogue` returns the original
384-byte Sources shape. The VIEW profile is
`keccak256("6529STREAM_VIEW_PRESERVATION_FINALITY_V1")`. Its configuration hash
commits the original constructor Config, provider, chain and complete binding
receipt. A deep memory copy prevents effective source replacements from
changing that original hash.

## Immutable selection and current evidence

Configuration reads the actual once-bound six-field VIEW selection and exact
historical complete receipt. It independently validates every current runtime,
original-derived source roster and reciprocal dependency. Initial dependency
hashes remain historical receipt fields: lawful reference read-budget changes
are checked against current dependencies rather than falsely rewriting that
receipt. Basic-only or pending bindings do not supply complete evidence.

Snapshot getters execute real nested validation. Their outer reads use the
existing `Config.sourceGas`; the getters' original inner read budgets remain
unchanged. Cheap factory and historical receipt getters keep `Config.readGas`.
The same complete currentness, original Router candidate, nine independent
component checks, actual inventory, full bundle coverage and two retained
manifest copies remain required. No cached readiness is introduced.

The shared inventory-anchor helper supports exactly the original raw dependency
commitment or the current-authority inventory's profile/Dependencies/O/D
commitment. The latter requires canonical originalAnchor equality, actual
immutable dependencyHash, O worker pin and finite budget, and the real
resolver's runtime, ERC165, profile and complete immutable anchors. It does not
read currentSelection or reinterpret historical binding receipts. The actual
inventory and O worker expose no ERC165 endpoint; none is invented for them.

## Manifest and review bytes

`STREAM_VIEW_PRESERVATION_FINALITY_INPUT_V1` and its
`STREAM_VIEW_PRESERVATION_FINALITY_INPUT_ABI_V1` canonicalization retain the
original ten-input bridge and nine complete ordered component expectations.
They explicitly add full source-set/policy commitments, snapshot/reference
profiles, complete-binding receipt, tagged adoption, output profile, complete
membership, full 28-word root binding and the separate declared viewId. The
portable definitions beside this guide match the exact registered bytes.
The original 8192-byte manifest bound and original Schema Store/Registry
staging checks remain.

Review schemaVersion1 profiles1 and2 retain their original meanings, predicates
and transport maxima. Profile3 is explicit VIEW preservation with nonempty
media and reference lists: each ordered list contains1..16 actual content
hashes, preserving repeated occurrences. The bound is inherited from the
original ceremony/schema vocabulary, not a reduced product limit. Canonical
responses at tuple bases32/160/416 have maxima1280/1408/1664 respectively.
Every consuming route requires canonical VIEW coordinates and the exact new
manifest schema/canonicalization. Unknown profiles remain unsupported.

The current VIEW payload grammar contains one optional image media occurrence.
Its actual completed artwork segment and bundle item/admission are rejoined,
and original complete Archive correspondence is checked again before the media
content hash is returned. URI text is not media bytes. Only supported exact
content correspondence can pass. Reference hashes come from the actual retained
reference publication and original external PNG objects, not caller lists.

## Evidence boundary

The frozen ABI72 capture is type/layout-only:305 sources, zero errors. There are
41 authored focused cases: six codec, six Candidate/prepared transport,
17 actual Schema/Store/Safe manifest cases with typed Core/Metadata/Registry
boundaries, eight immutable-anchor cases, and four configuration cases. They
have not been executed in this batch. The strict nested-cap control is a typed
model; actual bound-provider and current-authority inventory recipes are owned
by the combined-provider builders. No whole VIEW Artist/op17/Safe/finality
ceremony, deployment-size, transaction-cap or maximum-scope claim follows.

Inventory/source diagnostic budgets can exceed one transaction's16,777,216 gas
limit. They remain visible source-integration diagnostics. All original caps,
mandatory full reobservation and the unresolved maximum-scope freshness/capacity
requirements remain. This profile does not automatically admit the separate
current-Artist VIEW renderer successor.
