# VIEW preservation reference observations

`StreamViewPreservationReferencePublicationV1` publishes curator-attributed
BYTE_EXACT observations of the explicitly admitted non-sanction VIEW preservation
profile. It preserves the original engine/toolchain/file inventory, PNG, runtime
ZIP and independent Archive current-pair vocabulary. It has distinct VIEW record,
source, payload, history and lock domains. Existing COLLECTION and scoped V1
reference records are unchanged and cannot be substituted.

The source graph is constructor-pinned: Core, selected Metadata, Schema, Store,
selected Router, the new root-free VIEW snapshot publisher and external artifact
coverage. The host validates the snapshot's complete 768-byte dependency tuple
against the shared Core/Metadata/Schema/Store/Router identities. Current source
validation independently authenticates the full current snapshot and its exact
canonical payload, then joins the actual Router's scoped CONTENT_ROOT head and
its 28-word VIEW preservation binding. That Router getter verifies its original
record and historical aggregate; the reference source also recomputes the
distinct prepared state preimage and compares every binding field.

The root remains the original Artist operation17 transition in the Router's
single CONTENT_ROOT consent book. A renderer adoption alone cannot satisfy it.
The snapshot has no future root dependency, so the sequence is snapshot, genuine
CONTENT_ROOT adoption, then reference observation. The selected combined provider
must finish its once-only governed VIEW source binding before this sequence can
be accepted. A pending binding does not establish root or finality readiness.

One-token scopes require one capture; larger scopes require first and last
membership ordinals in that order. Each capture is matched to a freshly observed
complete 31-word checkpoint output, actual global token ID, collection serial,
current or burned-historical route, full JSON/HTML hashes and original-at-mint
entropy policy facts. Terminal policy output remains explicitly non-finalized
with zero seed; it is never recast as a finalized V1 token. Full scope membership,
ordered checkpoint rows and the covered complete manifest remain separate
mandatory dependencies. First/last captures never stand in for that inventory.

The original complete Environment serializer and authenticated file-inventory
preparation are retained. Permissionless preparation grants no authority. The
new schema describes the full typed publication, receipt and source tuple;
schema/profile/canonicalization definitions and the original Environment/PNG/ZIP
and format documents must have exact ACTIVE bytes. Archive reads bind each
object's identity and format. Current reads additionally require the original
current dual receipt/fixity pair.

Publication requires selected Metadata CURATOR class3, falling back to global
class8, an exact predecessor/revision, and an unused reference ID. It retains
complete canonical payload and input bytes in immutable Store chunks, then
rechecks writer grant and lineage before appending the receipt/history. A failed
retention or late check rolls the whole transition back. Historical bytes remain
readable after source drift. Current evidence reruns full source and payload
integrity checks; no previously staged row is promoted by a cached source hash.

`StreamFinalityViewPreservationReferenceReadsV1` independently checks the fixed
608-byte publisher dependency tuple, complete original record preimage and
672-byte receipt. Its locked/component paths require the original class2
current-action lock and exact scoped component commitment. This is a typed
adapter for the actual combined provider; it does not by itself wire the full
inventory, input manifest, sanction or Registry ceremony.

The authored tests use actual new Snapshot, Store, checkpoint, renderer and
reference contracts. They explicitly use typed Router/op17, Metadata grants,
Schema facts, covered manifest/Archive and governance boundaries. The separate
sample suite covers burned identity and full output row restoration. These are
source/type-check results until a captured native run is recorded. No browser
capture, full current-stack ceremony or universal gas-cap acceptance is claimed.
Complete-scope currentness still re-observes every row; no complete output
invalidation epoch exists to justify skipping those reads at large scope sizes.

The frozen ABI39 boundary type-checks 190 sources with zero errors. Its separate
152-source selected native size gate emits five products under the original
24,576-byte runtime and 49,152-byte creation limits:

| Product | Runtime bytes | Creation bytes |
| --- | ---: | ---: |
| Reference publication | 21,391 | 25,686 |
| Reference records | 23,763 | 23,796 |
| Reference source reads | 24,540 | 24,573 |
| Reference sample reads | 11,922 | 11,954 |
| Finality reference reader | 12,916 | 12,948 |

The source reader has only 36 bytes of runtime headroom. The evidence bridge
checks every source in the selected closure and 605 compiler metadata source
Keccak commitments. These are unlinked product sizes, excluding constructor
arguments. The seven sample and eight publication/reader cases are authored and
type-checked; they have not run in a captured EVM campaign. This selected gate
does not establish transaction capacity or complete combined-provider acceptance.
