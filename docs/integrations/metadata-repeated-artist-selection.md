# Original Metadata after repeated Artist migration

The same Metadata host can select an Artist successor after multiple complete
recovered operation60 imports. The original Artist, Metadata address, record hash
domain and consumed publication authorization map remain unchanged. This consumer
does not make an incomplete hydration profile support additional record families.

The original self-selection and single-predecessor path remain. The repeated path
first checks the actual current registry's one immediate history binding, exact
predecessor runtime and completed cutover naming the current registry. It then
reconstructs the original Artist's complete21-word recovery environment from its
pinned original suite: chain, Registry, Coordinator, Archive, all seven owners and
runtime hashes, Core, Manager and original full suite hash. All unchanged
non-Artist suite dependencies must still match the current suite.

The current actual Identity owner exposes an additive imported-only certificate:

```solidity
recoveredHydrationImportedOriginCertificate(bytes32 environmentHash)
    returns (bytes32 originHash, bytes32 importCommitment,
             uint64 importedAtRevision, uint8 ownerIndex);
```

This exact 128-byte response reads the original immutable `originIndexPlusOne`,
origin/era, profile and import fields. The unchanged complete operation60 producer
validates the full environment before installing those fields. Missing keys, local
origins, wrong owner/profile or an absent import fail. The full 672-byte origin
getter and all original prefix bytes remain available; this certificate neither
caches mutable source data nor creates another writer. Its commitment must equal
every one of the seven current owner completion markers.

The current suite remains authenticated against its exact original constructor
hash and all live runtime pins. For repeated selection, one fixed linked worker
then checks the exact immediate predecessor/cutover, original suite and all its
runtime pins, unchanged dependencies, complete original environment hash, current
Router/Core, seven completion markers and imported certificate. Metadata calls
that read-only worker with the unchanged governed dependency budget. The worker
shares that finite STATICCALL frame among these fixed reads and rejects every
failed or incorrectly sized result; it does not reserve the entire budget anew
before each small inner read. Inner source getters are the named original
caller-independent facts, and their caller is this fixed worker. There is no
caller-supplied target or generic batch execution interface. The worker's wire
uses ordinary `bytes` containing `abi.encode(nextSuite)` to avoid Solidity library
struct-selector mangling; it decodes the original typed suite once.

The outer full-forwarding/EIP150 guard is unchanged. At the measured genesis
configuration, the Artist callback is 400,000 gas and Metadata's dependency frame
is 150,000 gas. Neither value nor its floor is raised. This is a finite seven-owner
proof, not an unbounded ancestor walk or a guarantee that arbitrary future source
implementations fit those budgets.

A current successor publication still uses the original Metadata callback and
record domain. An authorization ID consumed before migration stays consumed at
later successors. Fresh publication requires the current Artist's original
publication authorization; ancestry alone does not authorize a record.

The candidate view now validates subject, schema, payload and original candidate
preimage before finishing with Artist selection. Each check is still mandatory.
For inputs with several defects, the first error can therefore change; individual
refusals are tested independently. A failed grouped read is reported against the
fixed worker. The publication mutation still captures selection before candidate
validation and rechecks after append, preserving authorization consumption and
rollback ordering.

The focused successor tests preserve the fourteen original immediate/repeated
recipes and add malformed/missing certificate, source-suite, independently invalid
document/selection and low-budget refusal controls. The repeated cold recipe uses
the actual immutable State producer and actual fixed Owner read worker in a typed
owner boundary; extra stored bindings and a refusal flag add conservative reads.
It cools the named original/current suites, payload and read libraries. Six separate
real Owner probes cover all seven indices, full retained origin bytes, exact
four-word output, local/missing refusal and reached late-failure rollback/retry.

Metadata, schemas, byte store, Safe and fixed read workers are real contracts.
Artist source admission, Coordinator construction and Core/governance remain
explicit typed boundaries. These tests do not execute the full seven-owner
operation60 or prove complete current-graph/cold-transitive deployment acceptance.
Earlier failed selector and gas-reservation captures remain retained separately.

## Recorded capacity boundary

The frozen169-source v4 cohort passes38 cases in total: eighteen successor cases
have17 passes and one cold failure, six real Owner certificate probes pass, and
all fifteen original Metadata cases pass (including256 payload fuzz inputs).
The repeated named-cold candidate still fails: its150,000-gas ancestry frame uses
149,669 gas before the actual certificate storage reader runs out of gas. The
outer400,000-gas candidate callback reverts after332,626 gas. This is a supported
source/ordinary behavior increment, **not acceptance of repeated cold publication
at the existing deployment budget**. The low-budget failure remains fail-closed.

The eighteen-product gate on the matched Artist/Metadata source has no runtime
or unparameterized creation-image overrun. Tight runtimes are IdentityWriter24,535,
Attribution23,947, Identity23,355 and Metadata24,433 bytes. The subsequent primitive
bytes wire fix changes only the two selection helpers; their actual v4 native
runtimes are4,076 and8,430 bytes. Metadata's matched native runtime remains24,433.
Constructor arguments must still be counted for each actual deployment. No gas
floor, dependency cap, callback cap, protocol size limit or import authority was
relaxed. The proposed two-frame successor is separately held and unapplied.

## Actual Owner read follow-up

A separate named-cold recipe now uses real Owner constructor bindings, original
Owner check/commit, actual hydration guard completion markers and the original
immutable prefix producer/State/Reads. Its Coordinator is explicitly typed: it
uses the original stored suite and immutable configuration/finality/provider
getter shapes, exact constructor hash and reciprocal owner bindings, but does
not execute the full operation55–60 source-admission lifecycle. Core, registry
history/admission and Archive remain typed boundaries. This improves the measured
read shape without claiming concrete Identity or full current-graph acceptance.

The certificate read now inlines the unchanged State view body inside its fixed
Reads library, retaining the original public State API, storage namespace and
all provenance predicates. Metadata also reuses the sixteen code hashes already
checked by the original configuration hash and the already-canonical suite hash
when reconstructing the origin. No mutating call intervenes. The original hash
API, literal constructor preimage and all code/return checks remain unchanged.

The first171-source follow-up passed42 of44 scoped cases, including both256-input
fuzz oracles, but still ran out of gas in the real Owner return. Its failures are
retained. Hash reuse increased the certificate allowance from22,403 to24,592 gas;
the fixed Reads body completed using20,294 gas but left too little for its wrapper.

The final narrow word-reader successor replaces temporary byte arrays only for
nine zero-argument full-word reads: the original binding count, source constructor
hash and seven completion markers. It retains nonzero code, exact32-byte return,
call success and all original value predicates. Address and structured decoders
are unchanged. Its scratch-memory STATICCALL uses the same remaining gas as the
old inner reader, within the identical single governed150,000 outer frame.

The final171-source capture passes all seven focused cases, including256 hash
fuzz inputs. The same cached retained cohort has45 passes and one preserved
conservative-fixture cold failure, including both256-input fuzz oracles. The real
Owner named-cold recipe succeeds: Metadata callback324,595/400,000 gas; single
ancestry frame147,146/150,000; certificate24,698 with27,669 forwarded. This is a
measured success for the explicitly described Owner/immutable-Coordinator recipe,
not complete current-graph or arbitrary future implementation acceptance. Only
2,854 gas remains in its dependency frame, so genuine full-suite composition
must still be measured. Missing-prefix and mismatched seventh-marker controls
fail for their intended predicates; short/long/zero-word, dirty-address and
missing-code controls require exact successful retries.

All82 concrete production products in that native closure fit the original
runtime and unparameterized creation limits. ABI comparison retains1,759 prior
entries and recursive storage layouts. The separate18-product Artist host gate
remains tied to its original recorded source; no full Artist rebuild is claimed.

The word-reader edit was initially rejected by automatic review on a per-read
cap premise. Exact old/new evidence showed both inner calls use remaining gas
inside the same outer cap; renewed review allowed the exact two-file patch.
The original denial, packet, transport normalization and successful application
record remain retained. The unrelated proposed two-frame partition stays inert.


## Cold fixture fidelity correction

The retained successor cost case now shares the actual Owner read fixture above.
Its former mutable boundary stored fields that are immutable in production and
charged for an extra mock-only enabled flag. Those mocks remain in every other
successor test for corruption and refusal controls; only the named-cold cost
case uses the actual Owner/Guards/State and immutable Coordinator recipe.
Original assertions, the 400,000 callback, the single 150,000 dependency frame
and all prior cooling remain; the actual Guards library is also explicitly cold.

The frozen 172-source follow-up changes no production source. The focused retry
passes, and the same cached retained cohort passes all 46 cases, including both
256-input fuzz oracles. The corrected case matches the separate real-Owner
measurement: 324,595 callback gas and 147,146 dependency-frame gas. All 82 concrete
production products in this unchanged native closure still fit. The original
conservative failure is preserved, as is an initial test-name filter that selected
no cases before the corrected cached retry. Neither is counted as a passing run.

The shared helper preserves the original classes and CREATE prediction/body;
this remains an Owner read-cost recipe with typed Coordinator, Core, history
admission and Archive boundaries. It does not execute complete operation 55–60,
deploy concrete Identity, or establish full current-stack acceptance. The narrow
2,854-gas dependency headroom still requires measurement on any new composition.
