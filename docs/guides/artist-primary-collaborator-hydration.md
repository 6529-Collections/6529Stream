# PRIMARY_ONLY collaborator hydration

This additive recovered-authority profile carries the original nonempty collaborator
state through the existing atomic operation60. Its tag is
`6529STREAM_ARTIST_PRIMARY_COLLABORATOR_HYDRATION_V1`, version1, feature8388608
in `StreamArtistExtendedHydrationFeatures`. Existing profile masks, Request,
Coordinator, signature domains and replay keys are unchanged. Capability is an
explicit owner union; knowing the bit alone does not advertise implementation.

## Supported original graph

The fixed preparation worker derives its selection from the actual predecessor's
owner1 revision/prefix and all retained binding terms. It then performs full
governed predecessor/cutover, lane, runtime, checkpoint and seven-owner admission.
Caller witnesses and expected capabilities never select a profile or subset.

Original ops5/6/7 are joined to the complete Archive catalogue and source maps.
The account-kind3 registration nonce tree is separate from each principal's
kind1 tree and the global identity allocation nonce. All three retain their exact
indices and bits, including sparse signatures, direct first-unused semantics and
successor prefixes. Empty owner1 native journals do not imply empty state.

Every original collection generation retains its ordered collaborator terms,
proposal, terminal outcome, primary acceptance and collaborator rows. Partial
primary or collaborator acceptance can precede refusal/withdrawal. Repeated
primary acceptance under a later authentic authority retains every native/Archive
occurrence while importing the actual latest primary map. Only that saved latest
primary record has a source timestamp; the codec invents no timestamp or authority
class for overwritten op2 records or op7. Completion uses the exact original
owner0/4 snapshots and record preimage, whether op2 or the final op7 completed it.

All seven original owner applies preserve the existing atomic commit. Imports
check target keys before writes and retain source provenance, publications,
runtime pins, complete nonce/replay inventories, timing and final Archive checks.
The original selected payout and attribution consumers continue to use the same
identity IDs, acceptance-ratified accounts and designation records.

Only PRIMARY_ONLY collaborator policy is accepted. This does not implement or
consume ALL_COLLABORATORS, THRESHOLD or COLLABORATOR_QUORUM policy, change a
held proposal, or invent present authority from historical signatures. Other
history families retain the inherited closed recovered class1/class3 scope;
this profile does not combine unbound Platform or aggregate-dispute profiles.

## Validation boundary

The authored focused suite contains twelve leaf/nonce cases and eight original
Registry/seven-owner/Archive/threshold-Safe scenarios. The latter inherit explicit
Core and scheduled-governance unit boundaries. They cover both completion orders,
repeated primary maps, historical partial refusal, malformed joined facts,
source drift, late original Archive rollback and repeated successor continuation.
No source alias or map is installed through a fixture write in those scenarios.

Quick ABI checks establish type compatibility only. Native code generation,
selected production capacity, execution of these cases and cold transaction/leaf
gas acceptance remain separate evidence. This source batch makes no full system,
testnet, audit or release-readiness claim. The local diagnostic captures remain
source-qualified; failed predecessor captures are retained.
