# Recovered pending binding generations

Composition base: `a6d8337f378c0db80454eea612d25cc1b0af719e`.
Branch: `codex/recovered-preparation-generations`.
Original feature packet: `bcea7e04e87b588283ebdd548835598304a99f3f`,
based on `71b8331eaabcadbe5af4aa6ec061c8db5c6f9189`.

## Bounded source profile

The integrator requested the next unsupported recovered class1/class3 collection
history. The source audit found complete original Identity25 already implemented
by the retained export, records and import workers. The adjacent missing profile
is pending binding generations: original proposal1, refusal3 or withdrawal4,
reproposal1 and one final acceptance2. Accepted bindings cannot be reproposed by
the original writer; this is not accepted-binding replacement or correction53.

The profile contains one Artist, one PRIMARY_ONLY collection, no collaborators,
mode1 in every generation, and2..128 generations. Every earlier generation has
one genuine terminal, and only the final generation is accepted. All Binding
mutations occur in the original era; later eras import its unchanged complete
history. Existing full Identity/Payout class1/class3 histories, including retained
registration and original25 documents, remain supported. Owner4 contains only
base Attribution, owner6 only direct14. Grants, mode2 and15/16/17/20/21/24 are
excluded from this bounded generation profile.

Actual source generation selects feature512; all seven concrete owners advertise
the explicit combined1023 mask. Existing31/63/127/255/511 constants and old codecs
remain unchanged. Request, entry points and Archive inventory composition keep
their prior shapes. Witness and royalty arrays are empty for this profile.

## Original state and proof

The owner0 codec collects every historical Binding, its exact terms and terminal,
plus the final current head. It independently reproduces each original proposal
hash from the original environment and no-collaborator terms. Complete proposal
and terminal mutations produce2N owner0 revisions. Only proposal and refusal
append native receipts; original4 is a state/replay mutation without a native
row. Exactly2N-1 distinct replay keys retain original admission coordinates and
each later era's complete rekeyed inventory. All four destination maps are
checked for pristine fields before any writes.

Owner4 uses a distinct generation tag but the original Attribution Bundle. Its
original revision is2N and each later import-only era has revision1, with no
native, replay or nonce inventory. Preparation joins its state2/generationN
to the complete accepted Binding before constructing all seven owner payloads.

Cross-owner facts bind every proposal's original identity hash to retained exact
document bytes. Each original refusal and final acceptance has exactly one
retained signature, including legitimate empty direct/Safe evidence. Original2/3
do not retain a record-keyed Identity admission or signer/nonce/deadline preimage.
The fixed original producer, exact source maps and full nonce/replay inventory
authenticate those facts. The importer does not invent them, reauthorize an old
signature, pair unretained digest preimages, or compare raw cross-owner revisions.

GenerationFacts retains its internal source call shape and projects only the
Identity artist/documents/signatures and three scope keys into a separate linked
GenerationFactRows worker. Complete Binding and Provenance bundles remain. The
validator body and check order are mechanically identical; this avoids repeating
the unrelated full Identity decoder at a public boundary. Selected native
compilation measures the codec at16,582 runtime bytes and the row worker at7,367.

## Authored regressions

The batch adds36 test declarations:16 codec cases,13 pure fact cases,4 actual
class1 cases,2 actual class3 cases and1 capability case. Old base fixtures are
unchanged. The old Simple gen2 export rejection remains correct for that old codec.

The codec component uses actual Binding and Acceptance writers under a typed
Coordinator. It covers mixed/refusal-only/withdrawal-only histories, complete
four-map import, literal original mutation/native/replay counts, malformed
rows/points/aliases/source checkpoints, occupied fields and late rollback with
identical retry. Real original writers produce128 and129 generations; only the
new collector rejects129. Its repeated-era certificate is explicitly synthetic.

Pure facts tests are partial certificates, not source authentication evidence.
They cover original documents/signatures, exact ownership and occurrence scope,
same-owner chronology, no invented Identity2/3/4 or owner4 rows,0/4096/4097 signature
bounds and2/128 versus1/129 generation bounds.

Actual class1 cases use genuine refusal/withdrawal/reproposal/final acceptance,
then33/35 recovery through original Registry, Coordinator, all seven owners,
Archive and Safe. Actual class3 cases use40/35 and mature estate authority with
full4095 or zero capability. They retain original and successor25 documents,
A-to-B-to-C history, exact source checkpoints captured after source57, original
signatures/nonces/coordinates and current policy actions. Class1 negatives begin
from valid preparation, reject wrong source/capability/witnesses and restore all
owners plus Safe nonce on a late Archive failure before identical retry. Class3
zero-rights cases retain the original2048 rejection and accepted-binding guard.
Core and governance dependencies remain declared typed fixtures.

## Preparation composition and validation

The original reviewed generation delta is frozen at+42/-4, SHA256
`8a5343015eff37e6776ee80a083234dc2aa1c02bbe18603cfb3b5ea2c10d9081`.
It is now composed into the fitted preparation stages in this same source batch.
The binding read and generation guards remain after evidence collection and
before economics and attestations. Generation one retains its early return;
later generations collect the complete original bundle, join its full Identity
facts, check accepted Attribution and select feature512. The cached consent mode
preserves later selection without an extra binding read. Owner0 encoding remains
after its original capability, publication and guard checks. All seven owners
receive the resulting feature mask. The Prepared public function bodies and its
six nominal selectors are unchanged.

A fixed typed GenerationFacts worker receives the full Identity bundle and
projects the same original document/signature rows and scope into the original
row validator. The fixed framing retains all Binding and Provenance fields and
bubbles exact revert bytes. Independent source review found no change to the
original generation checks, ordering or old preparation routes.

Shared Owner/OwnerPayload/Operations/Commit/ApplyGuards files are unchanged;
the imported-origin read certificate and pure payload repair remain with their
existing owners. Held ART26/class4 and collaborator/global-freeze changes are
outside this batch. Wider generation-aware record compositions remain work.

Independent source reviews are clear for the original codec, component/fact tests,
both actual-flow hosts and preparation/import/capability wiring, and for this
stage composition. The new preparation-stage host adds19 focused tests, including
eight fuzz cases, for exact ABI forwarding, generation-one early return, ordered
failure propagation, generation guards, complete row projection and cached mode
selection. Its source and dependency mocks test component boundaries; they do
not authenticate a real recovered source or prove the complete preparation flow.
Native execution of these19 tests is tracked separately and remains pending at
this source handoff.

Selected Solidity0.8.19 code generation uses optimizer200, viaIR, Paris and no
CBOR. The eight selected production products all fit runtime24,576 and creation
49,152 byte limits:

| Product | Runtime bytes | Creation bytes |
| --- | ---: | ---: |
| Preparation | 24,404 | 24,437 |
| PreparationGenerations | 9,541 | 9,573 |
| PreparationGenerationFacts | 3,212 | 3,244 |
| PreparationOwners | 15,151 | 15,183 |
| PreparationSelection | 909 | 941 |
| BindingGenerations | 16,582 | 16,617 |
| BindingGenerationFactRows | 7,367 | 7,399 |
| RecoveredCollectionHydration | 16,497 | 16,531 |

Preparation has172 runtime bytes of remaining capacity. The selected captures
use the exact production source contents in the final1,098-source ABI capture;
its input SHA256 is
`64a3b02a364bd2ce5b6c1d2c9d45006491f6c34775f5a1604910bfa3ab1acc82`
and output SHA256 is
`f1f85dff1df88688e647a43e1d73f76a25c144b86fc1556ab6f4780af84b623e`.
It has zero compiler errors and confirms exact Prepared ABI, storage and method
identifier parity with the fitted facade. The evidence report SHA256 is
`8eeda34e5bf9ee3ce1c7d0c86604ecec9b0887bf310c3d53b7a8b65388237928`.
These selected measurements do not establish capacity of the entire linked
deployment graph. Actual generation flows, whole-graph construction, gas, full
CI and release evidence remain with the integrator. This source delivery does
not establish ART34/35/36 or full-v1 completion.

## Original feature-packet evidence

The original ABI-only capture `recovered-generations-complete-v5` covers1,145 sources
with zero errors, including every recovered test host and its production imports.
Solidity0.8.19 uses optimizer200, viaIR, Paris and no CBOR, with ABI-only output.
Input SHA256:
`7c35b655c0d111e4a66215aa3e085cc61e15804faa88ef93efcb08d0a8a9db62`;
output SHA256:
`080b0ef07ddc708c208e182abc96ce7b7f4a3710967529e5bf6430358b0d2c53`.
Earlier failed API-name/parser captures remain separate. Final class3 host
SHA256 is `0f986a648debfd39c0ccc9e106b0ab1c03235c1e0072810048ccac19d8ee6993`;
its only changes after review were two formatter line wraps. Scoped formatting,
Markdown link17/17 unit tests, link/changelog and Windows-aware whitespace checks
pass. Compiler captures and the coordination patch remain untracked.
The original staging audit covered23 intended files: all19 Solidity sources match
the final ABI input after CRLF normalization,36 test declarations are added and
none removed, and no tracked changes remain outside the batch. Final independent
review also confirms exact GenerationFactRows body parity and the documented scope.
