# Actual preservation ceremony source checkpoint

This additive source follows [ADR 0054](../docs/adr/0054-explicit-non-sanction-preservation-rendering.md).
The original full-output construction and failing sanction-cycle ceremony remain intact.

## Actual graph and authored behavior

The new construction helper retains the original 37-role activation graph,
original static fallback products and original entropy factories. Separate
creation catalogues construct the preservation factories, provider and discovery.
Six direct construction cases retain eleven inherited activation cases.

The publication fixture uses real Core, Metadata, Router, threshold Artist and
governor Safes, delayed governance, paid mints, entropy finalization, original
Artist consents and class-2 seals. It deploys the actual preservation attribution
companion and renderer before the RendererRegistry fixes its full sorted target
roster. The original live renderer and current citation are admitted separately.
Preservation registration passes through the actual Registry and governance.

The source then builds complete output checkpoints and covered manifests,
original CONTENT_ROOT authority, locked snapshots and references, all six token
inventory stages and archive coverage of every retained source occurrence.
The source explicitly asserts unchanged preservation JSON/HTML and all nine
non-sanction component commitments across original sanction, canonical
finalization and permissionless sanction confirmation. It separately checks
that the live JSON displays the actual sanction record and authority class.

Five publication cases are authored: genuine activation/mints, missing original
root consent and retry, snapshot/export, replaced-producer currentness with
historical retention, and unadmitted-producer rollback with successful retry.
The full captured ceremony is an explicit external simulation entry point; it
is not counted as an executed or default passing test.

## Observation and validation boundaries

- The actual Registry consumes explicitly synthetic local analysis documents.
  Its golden vectors execute the genuine producer, but are computed by this
  fixture and do not represent an independent conformance review.
- The full ceremony requires fresh caller-supplied browser/environment/capture
  observations and package-member archive witnesses for its exact exported
  source bytes. Earlier HTML, generated PNGs and old captures are not evidence.
- Fixture observer/signing keys and archive endpoints are local test inputs.
  Real archival contracts and threshold Safe/governance calls do not turn those
  inputs into public archive attestations.
- Existing high fixture gas settings remain explicit. They do not establish
  acceptance within the 16,777,216 transaction gas limit.

The final construction, publication, provider/discovery, VIEW binding and legacy
VIEW source reader entries passed Solidity 0.8.19 ABI compilation over 2,094
sources in `artifacts/art27-gap3/preservation-ceremony-ea2c431-freeze`:

- Input SHA-256:
  `cacf8865851f855af49b052a8b651099a3b7cfe11f02d0843955b097edb48fb5`.
- Output SHA-256:
  `dd77e6eac0f5e3cb2ac50635ac13512de228cf497631ca6c86b441d415939342`.

Independent source reviews cover construction, the genuine admission base,
the six publication helpers, five publication cases and the final VIEW binding
source. There are 31 new cases: six construction, five publication and twenty
VIEW binding cases. The construction and binding suites also retain eleven
activation and nine provider/discovery cases respectively. No native execution,
browser capture, whole-ceremony result, gas acceptance or linked product-size
result is claimed.

## Independently owned dependencies

Borrowed source pins are recorded in local capture manifests:

- B producer/admission: `8c794f338bcf86238d1bd2ac1f09477bd50b5d99`.
- Root writers: `a1265c3a4108ebd6b24121107e5b5f26310599af`.
- Shared Router dispatch, VIEW content authorization and four governed route
  budget integrations: `ea2c431fbbcd381abf9a269e54bc906b42e68527`.
- Closed archive correspondence: `82c3b09d8e90025f765c7ebe5635d51efc0ac2a1`.
- C VIEW producer/checkpoint/manifest: `7701ad897c27257e5f082b310d62a6e35526cd0a`.
- C VIEW root-free snapshot: `b7fb101e59ff28317815d5402458a781256590b3`.
- The route-budget interface is the exact committed root interface from that
  shared dispatch source, including its inherited ERC165 capability.

The exact-source bridge matches 2,033 indexed source blobs and 61 borrowed
owner-commit blobs against the compiler input. The borrowed sources include all
four shared VIEW route joins and are excluded from this builder's commit.

These owner-controlled dependencies must be integrated with the ceremony. They
are excluded from this builder's source commit. The new VIEW snapshot binding
is separate from C's remaining VIEW reference, inventory and finality work.

## VIEW construction extension

The integrator selected a one-time class-2 pending-to-bound source transition
after the actual construction exposed a cycle: the provider precedes Finality,
while preservation attribution requires already deployed Finality. Original
constructors and runtime pins remain strict. The provider is created pending;
the real VIEW products are deployed after activation and then bound through the
original threshold Safe and governance executor. Pending source reads fail.

The binding commits an exact seven-field source tuple, the original six-word
VIEW declaration binding and initial snapshot dependencies. It has a permanent
receipt and allows no replacement. The original
COLLECTION/scoped source configuration remains fixed. Twenty focused cases
cover pending reads, original governance and receipt/event joins, rejected
contexts, source/worker runtime drift, historical receipt retention, nested
forwarding margins, declaration/policy capabilities, the 100k bootstrap getter,
downstream validator drift and bounded monotonic snapshot-budget growth. These
are authored cases; the ABI check does not execute them.

The actual fixture deploys the original CollectionViews host and admits it to
the real ModuleRegistry through the root Safe, then registers its canonical
manifest schema. It binds that declaration source and four deployed C products
through the original threshold Safe and class-2 executor, and reads back its
exact configuration, declaration, initial dependencies, worker pins and receipt.
The policy source factory is the existing scoped V2 factory from the provider's
constructor-pinned preservation recipe. Its main configured call caps
are 16m validation, 14m snapshot source, 12m manifest checkpoint, 9m checkpoint
serving and 3m/4m renderer/attribution. These satisfy necessary strict forwarding
margins. Whole-scope capacity, repeated reads, return costs and cold source
getter calls remain unmeasured.

## Governed route budget and currentness

The construction graph retains the original 30m Finality component budget; the
COLLECTION publication fixture uses its explicit 56m budget for its nested
40m/48m reads. Reusing those caps for scalar VIEW route reads cannot work inside
the bounded VIEW checkpoint and snapshot calls. The integrator's additive
`IStreamViewRouteReadBudgetV1` profile instead authenticates the original read
budget in the one-time governed declaration. This provider returns that exact
bound value directly after its pending check. The fixed shared consumers must
validate the profile, range, original global upper bound and equality to the
complete declaration; providers without the new capability retain old behavior.

The three snapshot capability getters authenticate saved receipt, original
authority, dependency identities, configurations, five worker pins and bounded
monotonic gas growth. Full graph and declaration validators still run at bind.
The fixed current snapshot, manifest and original adopted-source consumers
repeat all serving/currentness checks before use. The
[consumer guide](../docs/integrations/preservation-policy-consumers-v1.md#capability-checks-and-complete-currentness)
maps each extracted duplicate validation to its required downstream call. The
getters are authenticated capabilities, not a cached currentness verdict.

## Remaining actual VIEW ceremony joins

The pre-freeze fixture hook runs after actual STATIC activation, original
ratification, paid mints, terminal entropy, description schemas and IDENTITY
writer grant, before the Artist STATIC lock and Core freeze. An actual VIEW
snapshot also requires the original Artist presentation lock, so the genuine
presentation-lock batch runs before the hook and is not repeated later. The
SCRIPT/MEDIA/BASE_URI Artist content freeze remains after it. An actual VIEW
recipe must use that ordering for declaration and payload, locked membership,
the genuine scoped policy source set, renderer admission, original op17 adoption,
complete checkpoint/output archive coverage, snapshot publication and original
op17 root publication. A separately admitted Registry can include the late
VIEW serving contract in its fixed target roster. The earlier Registry's target
roster remains fixed.

Those publication joins and all cold getter, complete scope and transaction gas
measurements remain pending. C's reference, inventory and finality continuation
is independently owned and is not established by this source checkpoint. No
existing constructor, runtime pin, current source check or transaction limit is
relaxed by this fixture.
