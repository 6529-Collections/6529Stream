# Scoped full-policy provider and discovery source handoff

20 September 2026. Base: `02659a4112c5db14c163b0c27cf2c762189ec330`.
Branch: `codex/scoped-policy-publication-v2`.

## Source delivered

The additive combined provider retains the original three source configurations
and resolves TOKEN, RELEASE and SEASON policy V2 sources through the genuine
fixed publication factory. Its closed Router-profile selector verifies the exact
scope, source plan, child runtimes and root interpretation. The factory binding
and whole source configuration have explicit domains and immutable constructor
identities. A scope-specific graph is not a caller-supplied host catalogue.

Six typed workers consume V2 output, checkpoint, snapshot, reference, inventory
and bundle evidence. Exact constructor reciprocals and full-policy root facts
feed the original input manifest format and independent component flow. Original
Registry caller checks, Artist identity, canonical PNG review, WORK/RIGHTS seals,
conservation and snapshot locks remain required. Terminal zero-seed readiness
remains distinct from finalized randomness.

The matching discovery sibling preserves the original component interfaces,
order, Artist/Registry guards and three-profile branches. Its new scoped source
and reference pins come only from the current genuine factory graph. Constructors
validate fixed topology without requiring minted-scope children or publications.
Identity selection does not call complete inventory or component-state evidence.

The sole existing production file changed is the scoped-policy Router content
source reader. It validates the provider's scalar budget before using that
budget for scope-specific snapshot identity reads. The new discovery likewise
uses its configured component budget for provider selection. Original profile,
runtime, route and reciprocal checks remain in place. No Core or VIEW change is
part of this batch.

## Review and 27 authored regressions

Independent source review covered the new provider, graph selector, discovery,
factory reader, Router budget transport and typed workers. The copied provider
base was compared token by token with the original: only three metadata accessors
change from external to public virtual, enabling typed overrides.

The seven actual-provider cases create all seven genuine factory children and
exercise snapshot publication and actual Router adoption. They cover the cheap
scalar budget getter, insufficient graph transport and restored success, exact
source configuration, all three original catalogue entries, original COLLECTION
snapshot bytes, unknown profile, child runtime, full TOKEN scope and both exact
original Registry authorization errors.

The fourteen worker cases cover genuine root/snapshot/reference joins for TOKEN,
RELEASE and SEASON; every one of the 23 binding words; canonical original
projection versus current validation; terminal and finalized facts; actual
snapshot lock followed by all six STATIC families; literal Core hash preimages;
profile/constructor substitutions; ordered canonical PNG identities; and
Registry caller/runtime guards. Metadata-family cases deliberately stop at
missing original WORK/RIGHTS/conservation evidence rather than claim a complete
positive finality fixture.

The six discovery cases reuse the actual provider, factory children, snapshot and
Router root. A separate setup establishes the graph before the first test call;
the first route case does not warm the selector beforehand. They cover configured
versus insufficient nested transport, all three original catalogue tuples and
an untouched V1 TOKEN route, exact full-scope/profile substitutions, children from
another genuine scope's graph, actual
child and factory runtime drift, and construction against a fresh factory with
zero children. The original Router-candidate gate and adapter identities are
explicit boundaries. Cold-call and budget assertions are authored regressions,
not measured gas results.

Core observations, original profile hosts, adapters, Registry, Artist and late
record topology are explicitly named fixture boundaries. Worker statement cases
use actual inventory/bundle constructors with named completed-observation
boundaries. They do not establish a complete staged inventory or an end-to-end
Registry finalization ceremony.

## Validation boundary

The combined ABI-only capture contains 608 sources and zero compiler errors. It
includes all three new test files and the affected original scoped-policy Router
root tests, with their complete import closure. Settings are Solidity 0.8.19,
optimizer 200, via IR, Paris and no CBOR. Local capture:
`artifacts/art27-gap3/scoped-policy-finality-tests-v2/`.

| Evidence | SHA256 |
| --- | --- |
| Compiler input | `7759463b2bfd60ead55dec949f37c60d4d0c8ee77a1f27251ffd17e5152f8892` |
| Compiler output | `3cf5a7b6b77809e6bc1e195c27578abe744fed735174137bbcba3c04cd5f8cb5` |
| Actual-provider test | `e558a2dc960ab7ce44d4960595408173cd4c53a1d9cbb5ef79483d3be8ef421a` |
| Typed-worker test | `d3a206e6ba7136e2218d9baf28443c496748fd896731b3c36a8f1bbe9bbbc371` |
| Discovery test | `22a188a8e3e111dc97c866ccc782d46056ba09bff3626c60fa6e060d7735642e` |
| Router source transport | `59b65891d40b9f87da9166677669304c48aca29388064386375b121f589967fc` |
| Reviewed production source manifest | `51ac16a327ffec665a3f8b9b83b6889af4a32c9fb53cc0b2b6d2e1f364416a99` |

Scoped formatting and relative-import checks, 17 Markdown link tests, Markdown
links and the changelog gate passed. Draft captures are retained locally under
`artifacts/art27-gap3/`. A draft
identified the shared test VM's missing bytes4 expectation overload; the exact
Registry error assertion now uses the supported bytes overload.

No native code generation, runtime pass, bytecode size, transaction gas,
production readiness or full finality completion is claimed. Genuine native
acceptance and regenerated release evidence remain with the integration campaign.
See the [caller guide](../docs/integrations/scoped-policy-finality-v2.md).
